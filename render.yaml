services:
  - type: web
    name: safety-monitoring-backend
    env: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && python run.py dashboard
    envVars:
      - key: PORT
        value: 10000